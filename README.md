# Bonkasse

**This branch is not the active version. Use the [`easter-rewrite`](https://github.com/vindux/bonkasse/tree/easter-rewrite) branch instead.**

```bash
git switch easter-rewrite
```

`main` previously held the original desktop version of Bonkasse. It has been superseded
by the rewrite on `easter-rewrite` and its files were removed from this branch.

The old code is still in this branch's history — see commit
[`d23c28b`](https://github.com/vindux/bonkasse/commit/d23c28b5ca0affb649fbc6a5a8a103c0c9f27a9c),
or check it out locally:

```bash
git checkout d23c28b
```
