# Core dependencies (Tkinter is built into Python)

# Printer integration
python-escpos>=3.0.0          # ESC/POS printer integration
pillow>=10.0.0                 # Image handling for receipt graphics and euro note images

# GUI enhancements
tkcalendar>=1.6.1              # Calendar date picker widget

# Additional printer dependencies
pyserial>=3.5                  # Serial port communication for printers
pyusb>=1.2.1                   # USB printer communication
qrcode[pil]>=7.0               # QR code generation for receipts (optional)

# System dependencies for Windows printer support (optional)
# pywin32>=306                 # Windows-specific printer APIs (uncomment if needed)